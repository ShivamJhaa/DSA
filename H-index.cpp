#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define fastio ios_base::sync_with_stdio(false); cin.tie(NULL);

void solve(){
    int n;
    cin>>n;
    vector<int>arr(n);
    int ans = 0;
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
}

int main(){
    fastio
    int n;
    cin>>n;
    int i = 1;
    while(n--)
    {
        cout<<"Case #"<<i<<": ";
        solve();
        i++;
    }
  return 0;
}